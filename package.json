{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "Mobile Snake Game",
  "main": "index.html",
  "scripts": {
    "build": "npm run build:web && npm run build:android",
    "build:web": "mkdir -p www && cp index.html game.js www/",
    "build:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
} 